int max(int a, int b){
    int max;
    if(a>b){
        max = a;
    }else{
        max = b;
    }
    return max;
}
int min(int a, int b){
    int min;
    if(a<b){
        min = a;
    }else{
        min = b;
    }
    return max;
}
int dist(int a, int b){
    int low = min(a,b);
    int high = max(a,b);
    int x = min(a+b*a, b-a+a);
    int y = max(a*b%a, b-a-a);

    return high - low;
}